Model is {{@model.subscription.id}}
Subscription is {{@model.subscription.name}}

<Users::NewUser @subscription={{this.model.subscription}}>
</Users::NewUser>

<Users::UserList @users={{this.model.users}}/>

